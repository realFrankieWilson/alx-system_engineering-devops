sudo apt install ufw.	# installs the ufw command
sudo ufw default deny incoming	# denies all incoming traffic.
sudo ufw default allow outgoing	# allows all outgoing.

sudo ufw allow 22	# allows the SSH incoming traffic
sudo ufw allow 443	# allows the HTTPS SSL traffic
sudo ufw allow HTTP	# allows the HTTP traffic.

sudo ufw enable		# enables the firewall
